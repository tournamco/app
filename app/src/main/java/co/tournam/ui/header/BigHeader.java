package co.tournam.ui.header;

import android.content.Context;

public class BigHeader extends AbstractHeader {

    /**
     * The constructor for the Big Header class.
     *
     * @param context the current context
     * @param name    the name of the header
     */
    public BigHeader(Context context, String name) {
        super(context, name);
    }
}
